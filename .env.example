# -----------------------
# App / Environment
# -----------------------
ENV=dev

# -----------------------
# API
# -----------------------
API_HOST=0.0.0.0
API_PORT=8010
CORS_ORIGINS=http://localhost:5174
COOKIE_SECURE=false
COOKIE_SAMESITE=lax

# Generate a long random string for production
JWT_SECRET=qwertyuiop!12
JWT_ALG=HS256
JWT_EXPIRES_MINUTES=60

# -----------------------
# Database (Postgres)
# -----------------------
POSTGRES_HOST=localhost
POSTGRES_PORT=5434
POSTGRES_DB=pm_agent_os
POSTGRES_USER=pm_agent_os_user
POSTGRES_PASSWORD=pm_agent_os_password

# SQLAlchemy DSN used by the API
DATABASE_URL=postgresql+psycopg://pm_agent_os_user:pm_agent_os_password@localhost:5434/pm_agent_os

# -----------------------
# OpenAI (for later steps)
# -----------------------
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
GITHUB_TOKEN=ghp_your_token_here